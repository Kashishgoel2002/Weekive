---
/**
 * TEMPORARY development harness page for the persistence layer (Milestone 3).
 * Mounts the StoreHarness React island. Remove before launch (Milestone 9).
 */
import Layout from "../../layouts/Layout.astro";
import StoreHarness from "../../components/dev/StoreHarness.tsx";
---

<Layout title="Weekive · store harness">
  <main class="mx-auto max-w-xl px-6 py-12">
    <p class="text-sm text-muted-foreground">Temporary dev harness · Milestone 3</p>
    <h1 class="mt-1 text-2xl font-semibold tracking-tight text-foreground">
      Persistence &amp; useWeek
    </h1>
    <div class="mt-6">
      <StoreHarness client:load />
    </div>
  </main>
</Layout>
